[project]
name = "cloudy-intell"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.2.10",
    "langgraph>=1.0.10",
    "langsmith>=0.7.14",
]
